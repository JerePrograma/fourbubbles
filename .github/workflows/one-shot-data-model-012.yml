name: One-shot data model 0.1.2

on:
  pull_request:

permissions:
  contents: write

jobs:
  document:
    if: github.head_ref == 'agent/0.1.2-administrative'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/0.1.2-administrative
          fetch-depth: 0

      - name: Write model documentation
        shell: python
        run: |
          from pathlib import Path

          Path('docs/DATA_MODEL.md').write_text(r'''# Modelo de datos 0.1.2

## Convenciones

- UUID para entidades.
- `TIMESTAMPTZ` para instantes.
- `NUMERIC(15,2)`/`BigDecimal` para dinero.
- gramos enteros para peso.
- auditoría y versión optimista en entidades de negocio.
- bajas lógicas cuando existe historia referencial.
- Flyway como única autoridad del esquema.

## Identidad y auditoría

- `user_accounts`
- `user_roles`
- `refresh_tokens`
- `audit_events`

`audit_events` conserva entidad, identificador, acción, valores anterior/nuevo, motivo, actor y fecha. Se consulta mediante filtros administrativos.

## Clientes y domicilios

- `zones`
- `clients`
- `addresses`

### Invariantes de domicilios

- un cliente conserva al menos un domicilio activo;
- existe como máximo un principal activo mediante índice único parcial;
- `valid_from` es obligatorio;
- un domicilio activo tiene `valid_to IS NULL`;
- uno inactivo tiene `valid_to IS NOT NULL`;
- cambiar principal no borra el anterior;
- desactivar no elimina la fila, preservando pedidos históricos.

## Catálogo y precios

- `service_offerings`
- `garment_equivalences`
- `price_definitions`
- `promotions`
- `promotion_usages`

Servicios, equivalencias, precios y promociones poseen vigencia. Los datos comerciales iniciales se cargan por Flyway.

### Concurrencia promocional

La confirmación bloquea pesimistamente la fila de `promotions` y revalida los usos antes de insertar `promotion_usages`. El bloqueo serializa las confirmaciones competidoras para cupos y `one_per_address`.

## Pedidos

- `laundry_orders`
- `order_items`
- `order_state_history`

### Cantidades

- `physical_pieces`: piezas reales;
- `group_count`: grupos comerciales;
- `equivalent_units_applied`: unidades equivalentes;
- `declared_weight_grams` y `actual_weight_grams`.

### Precio

- `automatic_quoted_price`: cálculo automático original, inmutable como referencia;
- `quoted_price`: importe vigente, automático o manual;
- `confirmed_price`: importe congelado;
- `price_breakdown`: explicación JSON;
- `manual_quote_reason`, `manual_quote_at`, `manual_quote_by`: trazabilidad de ajuste.

La cotización manual no destruye el cálculo automático. La confirmación fija el importe vigente.

### Planificación

- `pickup_scheduled_at`;
- `promised_at`;
- `notes`.

Solo se editan antes de confirmar precio y durante consulta/cotización.

## Pagos

- `payment_methods`
- `payments`

Los pagos conservan pedido, cliente, medio, importe, moneda, fecha, referencia, notas, estado y auditoría. El saldo se calcula sumando pagos `PAID` y comparando con `confirmed_price`.

## Migraciones

- V1: identidad/auditoría.
- V2: zonas/clientes/domicilios.
- V3: catálogo/precios/promociones.
- V4: pedidos/estados/pagos.
- V5: configuración comercial inicial.
- V6: vigencia de domicilios y separación de cotización automática/manual.

## Pendiente de modelo

- recepción y evidencias;
- almacenamiento externo;
- compatibilidad;
- ciclos/máquinas/bolsas;
- rutas/paradas;
- caja/costos;
- inventario/equipamiento;
- reclamos/compensaciones;
- abonos/comercios.
''', encoding='utf-8')

          adr = Path('docs/adr/0004-administrative-history-and-locking.md')
          adr.write_text(r'''# ADR 0004: historia administrativa y serialización promocional

## Estado

Aceptado en 0.1.2.

## Contexto

Sobrescribir domicilios rompe pedidos históricos. Reemplazar el precio automático por uno manual elimina explicabilidad. Validar cupos promocionales solo al cotizar permite carreras al confirmar.

## Decisión

1. Los domicilios se versionan con `valid_from`/`valid_to` y baja lógica.
2. El principal activo se protege con índice único parcial y flush controlado.
3. El precio automático se conserva en `automatic_quoted_price`.
4. La cotización manual registra importe, motivo, actor y fecha.
5. La confirmación de promoción bloquea la fila promocional y revalida usos dentro de la transacción.
6. Los pagos y auditorías se consultan como historia, no como estado sobrescribible.

## Consecuencias

- Mayor trazabilidad y reproducibilidad.
- Migración aditiva compatible con bases existentes.
- Menor concurrencia por promoción durante confirmación, aceptable para el volumen inicial.
- La administración de domicilios y cotizaciones requiere reglas explícitas.
- Un futuro escalado podrá reemplazar el bloqueo por contadores atómicos sin cambiar el contrato funcional.
''', encoding='utf-8')

          Path('.github/workflows/one-shot-data-model-012.yml').unlink()

      - name: Commit model documentation
        shell: bash
        run: |
          set -euo pipefail
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add -A
          git commit -m "docs: document administrative history and promotion locking"
          git pull --rebase origin agent/0.1.2-administrative
          git push origin HEAD:agent/0.1.2-administrative
